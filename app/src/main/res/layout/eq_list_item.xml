<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

    </data>

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:padding="16dp">

        <TextView
            android:id="@+id/eq_list_item_magnitude"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="@color/text_black"
            android:textSize="18sp"
            android:gravity="center"
            android:textStyle="bold"
            tools:text="4.6" />

        <ImageView
            android:id="@+id/eq_list_item_arrow"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentEnd="true"
            android:src="@drawable/ic_chevron_right_gray" />

        <TextView
            android:id="@+id/eq_list_item_title"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="16dp"
            android:layout_marginEnd="16dp"
            android:textColor="@color/text_black"
            android:textSize="18sp"
            android:layout_toEndOf="@id/eq_list_item_magnitude"
            android:layout_toStartOf="@id/eq_list_item_arrow"
            tools:text="13 NW Honolulu" />

    </RelativeLayout>
</layout>